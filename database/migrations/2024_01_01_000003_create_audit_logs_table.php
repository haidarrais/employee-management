<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * Note: Using json() instead of jsonb() for MariaDB/MySQL compatibility.
     * Both provide binary JSON storage with validation. Laravel's json() method
     * creates a JSON column in MySQL/MariaDB which stores data in binary format
     * similar to PostgreSQL's jsonb, providing efficient parsing.
     */
    public function up(): void
    {
        Schema::create('audit_logs', function (Blueprint $table) {
            $table->id();
            $table->foreignId('user_id')->nullable()->constrained('users')->nullOnDelete();
            $table->string('action');
            // Using json() for MariaDB - provides binary JSON storage with validation
            $table->json('metadata');
            $table->string('ip_address', 45)->nullable();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('audit_logs');
    }
};